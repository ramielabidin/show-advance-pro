{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["-y", "supabase", "mcp", "start", "--project-ref", "knwdjeicyisqsfiisaic"],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"
      }
    }
  }
}
